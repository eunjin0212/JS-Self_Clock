function setNumPosition() {
  const nums = document.querySelectorAll(".number");
  for (let j = 0; i < nums.length; i++) {
    if (j + 1 === 3 || j + 1 === 6 || j + 1 === 9 || j + 1 === 12) continue;
    const angle = (j + 1) * (Math.PI / 6);
  }
}
